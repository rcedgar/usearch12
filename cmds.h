#ifndef A
#error "A not defined"
#endif

A(version)

A(allpairs_global)
A(allpairs_local)

A(cluster_fast)
A(cluster_otus)
A(cluster_smallmem)
A(closed_ref)

A(fastq_filter)
A(fastq_filter2)
A(fastq_join)
A(fastq_mergepairs)

A(fastx_orient)
A(fastx_uniques)
A(fastx_truncate)
A(fastx_get_sample_names)

A(makeudb_ublast)
A(makeudb_usearch)
A(makeudb_sintax)

A(sintax_summary)

A(udb2fasta)
A(udbinfo)
A(udbstats)

A(pairs_global)
A(pairs_local)

A(search_exact)
A(search_global)
A(search_local)

A(sortbylength)
A(sortbysize)

A(ublast)

A(uchime3_denovo)

A(unoise3)
A(uparse_ref)
A(usearch_global)
A(usearch_local)

A(sintax)

A(otutab)

A(search_16s)
A(udb2bitvec)

#undef A
