#ifndef A
#error "A not defined"
#endif

A(version)

A(cluster_fast)
A(cluster_otus)
A(cluster_smallmem)
A(cluster_mt)
A(closed_ref)

A(fastq_filter)
A(fastq_filter2)
A(fastq_join)
A(fastq_mergepairs)

A(fastx_orient)
A(fastx_uniques)
A(fastx_truncate)
A(fastx_get_sample_names)

A(makeudb_ublast)
A(makeudb_usearch)

A(sintax_summary)

A(ublast)

A(uchime3_denovo)

A(unoise3)
A(uparse_ref)
A(usearch_global)
A(usearch_local)

A(sintax)

A(otutab)

A(search_16s)
A(udb2bitvec)

#undef A
